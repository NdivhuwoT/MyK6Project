// K6 now handles reporting natively through handleSummary() function
// HTML reports are automatically generated when running tests
// The report.html and report.json files are created by K6 during test execution
// No additional processing needed!

console.log('K6 built-in reporting is now active. Reports are generated automatically.');